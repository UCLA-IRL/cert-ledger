rule
{
  id "logger-verification"
  for data
  filter
  {
    type name
    name /cert_ledger
    relation is-strict-prefix-of
  }
  checker
  {
    type hierarchical
    sig-type rsa-sha256
  }
}

rule
{
  id "logger-interest-verification"
  for interest
  filter
  {
    type name
    name /cert_ledger
    relation is-strict-prefix-of
  }
  checker
  {
    type hierarchical
    sig-type rsa-sha256
  }
}

trust-anchor
{
  type file
  file-name "cert_ledger-anchor.cert"
}